{
  "name": "example",
  "version": "1.0.0",
  "description": "Pipeliner Example",
  "main": "index.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "mocha --reporter json --reporter-option output=test-report.json test/*.js",
    "lint": "eslint . > lint-report.json"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "mocha": "^10.2.0"
  }
}
